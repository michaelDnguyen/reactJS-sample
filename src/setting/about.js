import React, { Component } from "react";
import { cpus } from "os";

export default class About extends Component {
  render() {
    return (
      <div>
        <h3>About</h3>
      </div>
    );
  }
}
